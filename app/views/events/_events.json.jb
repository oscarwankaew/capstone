{
  id: event.id,
  title: event.title,
  date: event.date,
  time: event.time,
  location: event.location,
  time_format: event.time_format,
  timezone_format: event.timezone_format,
  event_image: event.event_image,
  fighters: event.fighters,
  live_streams: event.live_streams,
  created_at: event.created_at,
  updated_at: event.updated_at,
}
